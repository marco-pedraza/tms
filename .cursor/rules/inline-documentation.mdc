---
description: Inline documentation standards
globs: *.ts,*.tsx
alwaysApply: true
---

# Inline Documentation Standards

> **📖 See `AGENTS.md` for complete code and documentation conventions**

## Essential Description

Avoid duplicating TypeScript types in JSDoc. Focus on "why" and "what", not "how". Document only when the purpose isn't obvious from the name and signature.

## Practical Examples

### Function

```typescript
/**
 * Creates a new user with the given name
 */
function createUser(name: string): User {
  // ...
}
```

### Hook

```typescript
/**
 * Hook for querying a resource by ID with cache integration
 *
 * Uses collection cache as initial data for immediate rendering
 * while fetching complete data in the background
 */
export default function useQueryResource({ resourceId, enabled = true }) {
  // ...
}
```

### Component

```typescript
/**
 * Form for creating or editing an entity
 *
 * Handles validation with Zod and state with TanStack Form
 */
export default function EntityForm({ defaultValues, onSubmit }: Props) {
  // ...
}
```

### Complex Logic

```typescript
/**
 * Normalizes a phone number to E.164 format
 *
 * Handles various input formats:
 * - Local numbers with country code detection
 * - International numbers with or without plus sign
 * - Numbers with spaces, dashes, or parentheses
 */
export function normalizePhoneNumber(input: string): string {
  // ...
}
```

## References

- See `AGENTS.md` section "Code Conventions" > "Documentation" for complete standards
