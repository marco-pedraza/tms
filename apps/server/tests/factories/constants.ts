/**
 * Offset value used to prevent ID collisions in test data across different factories.
 * Each factory uses this offset as a base to generate unique IDs for its test entities.
 */
export const ID_OFFSET = 1000;
